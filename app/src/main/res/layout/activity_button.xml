<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.sanat.seegreen.ButtonActivity"
    android:background="#000">

    <ImageButton
        android:layout_width="300dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="75dp"
        android:id="@+id/vision_button"
        android:src="@drawable/camera_jpeg"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:background="#fff"
        />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/vision_button"
        android:id="@+id/vision_text"
        android:layout_centerHorizontal="true"
        android:text="@string/vision_button_string"
        android:textColor="#fff"
        android:textSize="20dp"/>

    <ImageButton
        android:layout_width="300dp"
        android:layout_height="200dp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/vision_text"
        android:layout_marginTop="40dp"
        android:id="@+id/text_button"
        android:src="@drawable/chat_jpeg"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:background="#fff"
    />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_button"
        android:id="@+id/chat_text"
        android:layout_centerHorizontal="true"
        android:text="@string/text_button_string"
        android:textColor="#fff"
        android:textSize="20dp"/>



</RelativeLayout>
